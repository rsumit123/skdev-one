Plan: `/Users/rsumit123/work/skdev-one/docs/superpowers/plans/2026-08-23-breach-public-frontend.md`

## Parallel execution ledger

User explicitly requested frontend parallelization on 2026-08-24 because elapsed backend review time was too high.

Ruling: Parallel agents work in isolated Git worktrees/branches, never the same checkout; foundation Tasks 1–3 and experience Tasks 4/8 may proceed concurrently, then a dedicated integrator rebases/cherry-picks foundation first and reconciles the experience patch before gameplay Tasks 5–7 — the repository is a single-file app so isolation plus explicit integration is safer than concurrent edits in one worktree — if wrong, overlapping `index.html` edits may require a larger manual reconciliation pass.

Ruling: The visual direction preserves Breach's existing tactical command-console identity and battlefield canvas, with the signature element being a compact “daily battle authorization” strip that connects identity, remaining games, IST reset, and difficulty choice; public copy uses player-facing battle language and removes API/provider/storage jargon — this evolves the game instead of imposing a generic SaaS dashboard — if wrong, existing players may find the quieter public shell less technically expressive than the current benchmark console.

Ruling: Foundation branch owns the public state/API delimiter, credential storage, identity gate, auth screens, home/allowance/difficulty/matchup reveal, and their pure Node tests (Tasks 1–3). Experience branch owns first-time How to Play plus mobile/accessibility CSS and interaction primitives (Tasks 4 and 8) but must not invent backend/API state contracts or alter simulator rules. Integration resolves overlaps before Tasks 5–7.

Backend prerequisite status: all 9 backend tasks implemented locally; whole-backend final review still running; no production deployment yet.

Foundation stream: dispatched `/root/frontend_foundation` at base `bb414c0` in `/Users/rsumit123/work/skdev-one/.worktrees/breach-public-foundation` for Tasks 1–3 with TDD and per-task commits.
Foundation stream: implementation complete at `b011ec5` (`aa7dd753` Task1, `14447ca2` Task2, `b011ec5e` Task3); 20 tests, mandatory scan, bench gate +0.0pp, responsive browser matrix clean. Independent review `/root/frontend_foundation_review` dispatched against `bb414c0..b011ec5`.
Foundation stream: review found 3 Important issues (busy action/stale response race; post-boot expired credentials retained; guest-token storage exception outside sanitized boundary) plus technical meta copy. Fix round 1 commit `4af3533`; 27 tests, browser race/expiry/storage probes, mandatory scan, bench +0.0pp clean; scoped re-review dispatched.
Experience stream: dispatched `/root/frontend_experience` at base `bb414c0` in `/Users/rsumit123/work/skdev-one/.worktrees/breach-public-experience` for Tasks 4 and 8 with TDD, visual/accessibility checks, and integration hooks.
Experience stream: commits `7e9cd92` (Task 4) and `4fa89b9` (Task 8); independent review found 4 Important issues (backward focus-trap leak, non-stack-safe nested/rotate focus, foundation onboarding/replay hook not wired, narrow copy/touch audits) and 2 Minor issues (Close/Escape not marking automatic onboarding seen; storage failure suppresses help). Reviewer independently verified all required viewport fits and `bench.js gate` PASS with unchanged health warnings; implementer report's earlier gate-failure note is stale.
Experience stream: branch-local fix is queued; foundation hook wiring and add/add/index conflicts will be resolved by integration after Tasks 1–3.
Experience stream: fix commit `47c9045` addresses focus trap/stack/rotate, hook/mount API, broader audits and 44px controls, automatic dismissal seen semantics, and storage fallback; 10 tests, five-view browser matrix, scans, sim prefix and bench PASS. Independent final re-review dispatched; integration must call `onPublicIdentityResolved` from foundation acceptance and reconcile public test/title conflicts.

Integration stream: foundation commits `aa7dd753`, `14447ca2`, `b011ec5e`, `4af3533`, and `678cfc05` were cherry-picked first, followed by experience commits `7e9cd92`, `4fa89b9`, `47c9045`, and `8574a2e`. Conflict resolution retained the foundation tactical public shell inside the broader experience `PUBLIC MARKUP` audit boundary, combined `applyMode` through `setRotateDialog` with the exact `LIVE && portrait && narrow && !FORCED` gate, merged both Node suites, kept one idempotently mounted How to Play menu action, and calls `window.breachPublicHooks?.onPublicIdentityResolved(PUB.identity)` from `renderPublicIdentityHome()` after public session acceptance.

Integration verification: `node --test war-game/public-app.test.js` passed 46/46; the mandatory scan reported `SYNTAX OK | dup fns: none | secret: clean`; the 24,416-byte simulator prefix through `3b. COMMANDERS` is byte-identical to `bb414c0` with SHA-256 `0a2976f098badf5d594862254ce7c27564f07334341875c06bae82a4426aa3cb`; `node war-game/bench.js gate` exited 0 with `GATE: PASS` and every row `+0.0pp`. A runtime-only same-origin API fixture exercised identity → guest → first-run onboarding → home → Medium matchup and logout/signup without repository changes. Browser checks passed at 844×390, 740×360, 932×430, 390×844, and 430×932: no horizontal overflow, visible controls at least 44px, compact onboarding and matchup fully fit, portrait home/signup fully fit, native signup validation focused `signupName`, the permanent replay action appeared exactly once, the rotate prompt stayed absent outside `LIVE`, and the console recorded no warnings or errors. No push or deployment occurred.

Owner Lab stream: Task 7 implemented on isolated branch `feature/breach-owner-lab` from exact integration commit `04d766a`. Visibility and every start helper use only `/v1/me` identity `kind === 'user' && isAdmin === true`; owner-looking names/emails do not grant access. Real Human/Model and Model/Model starts send the strict six-field camelCase payload to `POST /v1/admin/games`, apply only the server response, use `server` commander seats, tolerate server 403 without entering live play, and discard a delayed response after logout. The owner ruling for the backend-contract mismatch keeps Mock as a distinct admin-only local Mock-vs-Mock diagnostic: it makes no admin-game request, consumes no quota, sets no `PUB.game`, and remains unrecorded.

Owner Lab verification: 59/59 Node tests passed; mandatory syntax/duplicate/secret scan clean; `git diff --check` clean; `node war-game/bench.js gate` exited 0 with `GATE: PASS` and every drift row `+0.0pp`. Runtime-only same-origin fixtures verified guest 844×390 and user 740×360 hide lab/setup affordances with no overflow; admin 932×430 and 430×932 show the lab only through the account menu, keep the full-screen modal bounded with internal scrolling and 44px header actions, contain no browser credential/results-server fields, and log no console warnings/errors. A successful real start made exactly one admin request and entered live; a 403 remained in the lab with sanitized copy and never entered live; local Mock entered diagnostic play without increasing the admin-request counter. No push or deploy occurred.

Final assembly: COMPLETE. Resumed by a separate (non-codex) agent on 2026-08-24. Created `feature/breach-public-complete` from integration commit `04d766a` (Tasks 1-4,8), cherry-picked gameplay/results `8f9168e` (Tasks 5,6 — clean) then owner-lab `ed9a2a9` (Task 7). Three add/add conflicts reconciled by union: `setPublicHidden` + `syncBenchmarkLabAccess` both kept (both used/tested); the slice-export list unioned; the DOM-slice test context gained `canOpenLab` and exports both `setPublicHidden` and `syncBenchmarkLabAccess`. Verified: `node --test war-game/public-app.test.js` = 71 passed / 0 failed; mandatory scan `SYNTAX OK | dup fns: none | secret: clean`; `node war-game/bench.js gate` = PASS with every row +0.0pp (sim slice untouched). Tip `758f691`. All frontend tasks 1-8 now on one branch. REMAINING: Task 9 (docs, production cutover, live verification). NOT pushed, NOT merged to main, NOT deployed.

LAUNCH: LIVE 2026-08-24. Backend (feature/breach-accounts-backend @ 0e2cd27, incl. final-fix-wave commit 4df16cb + provider-smoke tolerance fix) deployed on ssh-social in ~/breach-api-new (compose project breach-api, same volume, port 8050, compatibility mode reads+writes=true). DB backed up (98 matches, integrity ok) to ~/breach-backups/. Additive migration ran on startup (users/guests/daily_usage/game_sessions/auth_sessions/model_health/provider_events added; 98 matches preserved). Real provider smoke = PASS. Public API verified live (guest->me->difficulties; leaderboards). Frontend merged to main (95ff3b1) + docs (fd2a2d1), pushed; Vercel serving the public shell ("Choose your difficulty", data-public-screen, wired to breach-api.skdev.one). REMAINING (manual/owner): (1) owner registers rsumit123@gmail.com via public signup, then run scripts/assign_owner_matches.py; (2) after confirming public play in-browser, flip BREACH_LEGACY_PUBLIC_READS/WRITES to false in ~/breach-api-new/.env and docker compose up -d to close the compatibility window; (3) full predeploy_gate ceremony was intentionally skipped (no real users) — run it for any future high-stakes redeploy.
